---
interface Props {
	title: string;
	icon: string;
}

const { title, icon } = Astro.props;
---


<div class="
    flex flex-wrap gap-3 items-center
    border-b-2 border-white mx-4 mb-4 px-4 pb-2
">
    <i class={icon}></i>
    <h2 class="uppercase font-extrabold ">{title}</h2>
</div>
